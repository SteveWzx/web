require.config({
    baseUrl: "../",
    paths: {
        "jquery": "lib/jquery-1.11.1",
        "artpl": "lib/template"
    }
});

define(function(require){
    var $ = require('jquery'),
        Food = require('src/render');

});
